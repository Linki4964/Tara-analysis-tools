{
  "name": "tara-asset-identification",
  "version": "1.0.0",
  "description": "TARA (Threat Analysis and Risk Assessment) Asset Identification Tool for Automotive Cybersecurity",
  "private": true,
  "scripts": {
    "dev:backend": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "start:backend": "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "frontend"
  ],
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
